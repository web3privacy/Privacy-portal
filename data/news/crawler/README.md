# Crawler data

- `candidates.json` – articles waiting for admin approval (generated by scripts/crawl-news-feeds.mjs)
- `images/` – downloaded images for candidates (referenced by relative path in candidates)
- `rejected.json` – set of candidate IDs that were rejected (to avoid showing again)

/**
 * Entry point for GlobalFooter only. Use this in apps that do not use Next.js
 * (e.g. Vite + React) to avoid pulling in next/link from the main package.
 */
export { GlobalFooter } from "./global-footer";
export { GlobalFooterSocialIcon } from "./global-footer-social-icon";
export type {
  GlobalFooterProps,
  GlobalFooterConfig,
  FooterLink,
  FooterSocialItem,
} from "./global-footer-types";
